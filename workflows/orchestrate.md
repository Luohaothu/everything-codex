# Workflow: Orchestrate

Use for larger tasks requiring multiple passes.

## Suggested Sequence
1. Planning pass (`workflows/plan.md`)
2. Implementation pass (`workflows/tdd.md`)
3. Review pass (`workflows/code-review.md`)
4. Final gate (`workflows/verify.md`)
